# Core dependencies for the AI UI testing pipeline (lightweight)
# Install this for day-to-day development and running the pipeline with remote LLMs (OpenAI) or dummy embeddings.

# Core pipeline & runtime
langchain
llama-index
openai
playwright
chromadb
pydantic
fastapi
uvicorn
pytest
allure-pytest
python-dotenv

# Celery / background workers
celery
redis

# Document format + parsing
python-docx  # DOCX support
pandas       # CSV / Excel parsing (optional but recommended)
openpyxl     # Excel xlsx backend for pandas
beautifulsoup4
requests

# FastAPI file upload support
python-multipart

# OCR & PDF helpers (optional - only needed for scanned PDFs / OCR fallbacks)
Pillow
pytesseract
PyMuPDF      # fitz - rendering PDF pages for OCR
pdf2image    # alternative PDF->image path (requires poppler installed on the system)

# Local/CPU light inference (optional)
# (If you want to run heavy local models, install requirements-optional.txt)

# Dev / extras (small)
python-multipart

# Note: heavyweight LLM training / inference libs (transformers, vllm, torch) have been moved to requirements-optional.txt
# to avoid long installs and platform-specific wheel issues during typical development.
